{
  "_comment": "Manual alternative to the plugin: merge this into .claude/settings.json of the working project (not of FAW) ONLY if FAW is not installed as a plugin, since the plugin already registers these hooks. The 'hooks' block itself can be versioned with the repository. What is per-machine is the path to FAW: replace <faw> with the real path of the clone on each machine.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python <faw>/faw/hooks/commit_gate.py"
          }
        ]
      },
      {
        "matcher": "PowerShell",
        "hooks": [
          {
            "type": "command",
            "command": "python <faw>/faw/hooks/commit_gate.py"
          }
        ]
      }
    ]
  }
}
